
import { Grid } from "@mui/material";
import { display } from "@mui/system";
import React from "react";


import Post from "./Post";
const ListPost = ({ posts }) => {
  return (
    // <Grid     >
    //   <h2 className='section-title'> List</h2>
    //   <Grid style={{display:'flex', flexWrap: 'wrap', flexDecoration: 'row'}} item xn='4' >

    //   {posts && posts.length && posts.map((item, index) => {
    //       const { picture, name, price, id } = item;

    //       return (
          
    //         <ListProduct
    //           picture={picture}
    //           name={name}
    //           price={price}
    //           id={id}

    //         />
    //       )
    //     })}
    //   </Grid>
    

        
     

    // </Grid>
    <div></div>
  )
}

export default ListPost